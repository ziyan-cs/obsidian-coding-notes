# 2. 关机 & 重启

- 关机
	- `halt`：立即关机
	- `reboot`：立即重启
	- `sync`：同步内存数据到磁盘
	- `poweroff`：立即关机

- 重启
	- `shutdown`：默认一分钟后关机
	- `shutdown -h now`：立即关机
	- `shutdown -h 1`：一分钟后关机（可自定义时间）
	- `shutdown -r now`：立即重启

# 3. 运行级别

- 传统运行级别：
	- `0`：关机
	- `1`：单用户模式【找回丢失密码】
	- `2`：多用户状态无网络服务
	- `3`：多用户状态有网络服务
	- `4`：系统未使用保留给用户
	- `5`：图形界面（默认）
	- `6`：系统重启
	- `init [级别]`：切换运行级别（例如  `init 3`）

- 现代 *Ubuntu* 等系统用 `systemd 目标（target）` 替代传统运行级别，`init` 命令仅做兼容：
	- `0` ↔ `poweroff.target`（关机）
	- `3` ↔ `multi-user.target`（多用户命令行，服务器常用）
	- `5` ↔ `graphical.target`（图形界面，桌面常用）
	- `6` ↔ `reboot.target`（重启）
	- `systemctl get-default`：查看当前运行级别
	- `systemctl set-default 对应运行级别` ：切换到对应运行级别
