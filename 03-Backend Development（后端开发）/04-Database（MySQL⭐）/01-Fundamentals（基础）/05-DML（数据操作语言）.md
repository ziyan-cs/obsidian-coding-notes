
# 数据增删改

```sql
# 增：插入数据
insert into [表名](字段1, 字段2) values (值1, 值2);

# 删：删除数据
delete from [表名] where [条件];

# 改：更新数据
update [表名] set [字段1]=[值1] where [条件];

# 改：更新所有数据
update [表名] set [字段1]=[值1] where [条件];
```