

（适配：R4S、单网口、后续要接 Windows 电脑共享上网）
## 一、物理接线（初始化唯一接法）

1. 软路由断电
2. **一根网线**：电脑网口 ➜ 软路由 LAN 口
3. 不要插 WAN、不要插其他设备、不要 U 盘

---

## 二、电脑端 手动设置静态 IP（为了连上初始化路由）

1. 电脑打开：控制面板 → 网络和共享中心 → 更改适配器设置
2. 找到「以太网 / 有线网卡」→ 右键属性 → IPv4 协议
3. 手动填写（固定模板）

```shell
IP地址：192.168.1.2
子网掩码：255.255.255.0
网关：留空
DNS：留空
```

确定保存

---

## 三、软路由上电 + 首次后台登录

1. 软路由通电，等待 60 秒完全启动
2. 电脑浏览器输入默认后台地址：

```shell
192.168.1.1
```

3. 登录信息（OpenWrt 官方默认）

- 用户名：`root`
- 密码：**无密码**，直接回车登录

---

## 四、进入后台第一件事：修改 LAN 基础地址（关键）

路径：网络 → 接口 → `lan` 编辑

```
协议：静态地址
设备：br-lan （默认别动）
IPv4地址：192.168.137.3/24
IPv4网关：【完全清空】不填
```

保存，**先不点 保存并应用**

---

## 五、设置 WAN 口（单网口必选配置）

路径：网络 → 接口 → `wan` 编辑

```
协议：DHCP 客户端
设备：改为 br-lan （单网口唯一写法）
高级设置：
✔ 用该接口网关作为默认网关
DNS自定义：114.114.114.114 , 223.5.5.5
```

保存 → 保存并应用

---

## 六、修改电脑 IP，适配新路由网段

软路由 LAN 已经改为 `192.168.137.3`

电脑有线网卡 IPv4 改为：

```
IP：192.168.137.2
掩码：255.255.255.0
网关/DNS：全部清空
```

保存

---

## 七、二次登录后台（以后长期用这个地址）

浏览器固定输入：

```
http://192.168.137.3
```

从此**永久用这个 IP 管理软路由**，不再变动。

---

## 八、开启电脑 WiFi 共享（让软路由上网）

1. 电脑连校园网 WiFi，保证电脑本身能上网
2. 网络适配器 → 右键你的 WiFi → 属性 → 共享
3. 勾选：
    
    「允许其他网络用户通过此计算机的 Internet 连接来连接」
4. 下方选择：你插软路由的**有线以太网网卡**
    
    确定

> 开启后，电脑有线网卡自动变为：
> 
> 网关地址：`192.168.137.1`

---

## 九、初始化完成检测（确认完全正常）

软路由后台 → 系统 → 终端

依次输入测试：

```shell
ping -c 3 192.168.137.1     # 测电脑连通
ping -c 3 114.114.114.114   # 测外网连通
```

全部能收到延迟 = 初始化 + 上网 100% 完成。

---

## 极简总结（以后重置 / 重装直接背）

1. 电脑 IP：`192.168.1.2`，进 `192.168.1.1`
2. LAN 改：`192.168.137.3/24`，网关空
3. WAN 改：DHCP + 绑定 br-lan
4. 电脑 IP 改：`192.168.137.2`
5. 开 WiFi 共享，完成上网