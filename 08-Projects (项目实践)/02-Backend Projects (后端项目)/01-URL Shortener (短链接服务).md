---
tags: [project/url-shortener, backend/api]
status: seed
review_due: 2026-10-05
confidence: 1
verified: 2026-09-05
---

# 01-URL Shortener (短链接服务)

> [!abstract] 学习定位：目标：用户创建短链接，访问短码后重定向；重点是读写路径、冲突、缓存与统计边界，不是生成一个随机字符串。

## 30 秒回答

**核心结论**：目标：用户创建短链接，访问短码后重定向；重点是读写路径、冲突、缓存与统计边界，不是生成一个随机字符串。


## 最小范围

- `POST /v1/links`：校验 URL，创建短码。
- `GET /{code}`：查询并 302 redirect。
- `GET /v1/links/{code}`：读取元数据。
- 不做登录、多地域、复杂分析；这些作为后续扩展。

## 关键问题

| 问题 | 第一版选择 | 需要验证 |
| --- | --- | --- |
| 短码冲突 | 数据库唯一约束 + 重试 | 冲突率与最大重试次数 |
| 热点读 | Redis cache-aside | 缓存未命中与失效行为 |
| 无效链接 | 显式 404 / 410 语义 | 不泄漏内部信息 |
| 统计 | 异步事件（后续） | 至少一次投递的去重 |

## 验收

- [ ] Go 标准库 HTTP API + `httptest`。
- [ ] MySQL 唯一约束、Redis 缓存、timeout 和结构化日志。
- [ ] Python checker 生成链接、验证 redirect、汇总延迟。
- [ ] 一张读路径图、一份压测报告、一条缓存失效复盘。

## 30 秒项目表达

我用短链接服务练习“写入正确性和读路径性能”的分离：短码由唯一约束兜底，读请求走 cache-aside，缓存失效时仍以数据库为 source of truth。第一版只证明重定向、冲突处理、超时和观测，不把多地域或分析系统伪装成已实现能力。

> [!warning] 常见误区
> hash 截断不等于永不冲突；302/301 的选择、负缓存和热点保护都需要以真实需求与测试结果决定。



## 从零建立模型

本页主题是 **01-URL Shortener (短链接服务)**。项目笔记要先把“用户问题—接口契约—数据变化—失败处理—证据”连成一条线。功能清单只是起点；如果不能说明失败时系统会做什么、如何观察，就还不是可交付的工程设计。

## 最小实践

选择一条主请求，画出输入、验证、状态变化、下游调用和响应。再人为让一个下游超时，写出重试、用户可见结果和日志/指标应该如何变化。

## 工程检查点

不要以中间件名词代替设计理由。技术选型必须能回到容量、延迟、一致性、安全或运维约束，并能留下一条可复现实验或测试证据。



## 渐进练习

1. **第一步 · 理解**：读：用一条用户请求串起接口、数据变化和下游依赖。
2. **第二步 · 实现**：写：列出一个失败场景（超时、重复或库存不足）与期望对外响应。
3. **第三步 · 验证**：证：保留测试、日志或压测截图对应的命令与结论，形成可复述证据。

## 常见误区

- 先堆功能、后补边界，导致接口、数据一致性和失败处理都无法验证。
- 缺少测试、压测、日志或复现步骤，项目难以作为工程能力证据。

## 学习闭环

### 从零复述

- 不看正文，用“问题 → 机制 → 边界”三句话讲清 **01-URL Shortener (短链接服务)**。

### 最小验证

- 写一个最小代码、命令、测试或项目观察，亲自验证本页的一条关键结论。

### 自测

1. 它解决的工程问题是什么？
2. 核心机制在哪个环节生效？
3. 什么时候应当换用另一种方案？
