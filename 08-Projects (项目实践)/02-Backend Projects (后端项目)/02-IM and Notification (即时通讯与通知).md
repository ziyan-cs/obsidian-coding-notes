---
tags: [project/im, backend/concurrency]
status: seed
review_due: 2026-10-05
confidence: 1
verified: 2026-09-05
---

> [!abstract] 一句话结论：即时通信要分开处理连接生命周期、消息投递与用户已读；每层都有独立的失败与重试语义。

# 30 秒回答

**核心结论**：心跳只能推断连接可能存活，broker ACK 只能说明投递或消费阶段，已读回执才是用户看到的业务证据；三者不能互相替代。


# 最小范围

- 单机 WebSocket / TCP 连接管理。
- 心跳、登录/登出、单聊消息、离线消息占位。
- 不做端到端加密、群聊大规模 fan-out、多机全局顺序。

# 必答问题

- 连接断开怎么检测？心跳的误判成本是什么？
- producer 重试后如何避免重复消息？
- 消息 ID、会话 ID、顺序范围分别是什么？
- 服务关闭时怎样停止接入并处理在途消息？

# 单聊的最小数据契约

```json
{"message_id":"uuid","conversation_id":"c42","sender_id":"u1","sequence":18,"body":"hello"}
```

`message_id` 用于去重；`conversation_id + sequence` 用于定义一个会话内的顺序范围；`sender_id` 用于权限和审计。服务端写入消息后再通知接收方，客户端重连时按最后确认的 sequence 拉取缺口，不能依赖一次 WebSocket 推送必达。

# 验收

- [ ] 连接状态机图：connecting / active / closing / closed。
- [ ] 模拟慢客户端和断线重连。
- [ ] 写出幂等消费/去重方案与局限。
- [ ] Python 脚本模拟多客户端，不将测试当作真实性能数据。

# 30 秒项目表达

这个项目把“TCP/WebSocket 连接管理”和“消息可靠投递”拆开建模：连接状态机处理生命周期，消息 ID 与幂等键处理重复，离线与重试必须说明一致性边界。重点不是承诺全局顺序，而是明确顺序保证在哪个会话范围内成立。

> [!warning] 常见误区
> “客户端收到 ACK”不必然代表用户已看到消息；重连也不天然保证不会重复投递。



# 从零建立模型

本页主题是 **02-IM and Notification (即时通讯与通知)**。项目笔记要先把“用户问题—接口契约—数据变化—失败处理—证据”连成一条线。功能清单只是起点；如果不能说明失败时系统会做什么、如何观察，就还不是可交付的工程设计。

# 最小实践

选择一条主请求，画出输入、验证、状态变化、下游调用和响应。再人为让一个下游超时，写出重试、用户可见结果和日志/指标应该如何变化。

# 工程检查点

不要以中间件名词代替设计理由。技术选型必须能回到容量、延迟、一致性、安全或运维约束，并能留下一条可复现实验或测试证据。



# 渐进练习

1. **第一步 · 理解**：读：用一条用户请求串起接口、数据变化和下游依赖。
2. **第二步 · 实现**：写：列出一个失败场景（超时、重复或库存不足）与期望对外响应。
3. **第三步 · 验证**：证：保留测试、日志或压测截图对应的命令与结论，形成可复述证据。

# 常见误区

- 先堆功能、后补边界，导致接口、数据一致性和失败处理都无法验证。
- 缺少测试、压测、日志或复现步骤，项目难以作为工程能力证据。

# 学习闭环

## 从零复述

- 不看正文，用“问题 → 机制 → 边界”三句话讲清 **02-IM and Notification (即时通讯与通知)**。

## 最小验证

- 写一个最小代码、命令、测试或项目观察，亲自验证本页的一条关键结论。

## 自测

1. 它解决的工程问题是什么？
2. 核心机制在哪个环节生效？
3. 什么时候应当换用另一种方案？
